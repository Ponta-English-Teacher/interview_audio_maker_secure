{
  "version": 2,
  "builds": [
    { "src": "api/tts.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/tts.js" },
    { "src": "/(.*)", "dest": "/public/$1" }
  ]
}
